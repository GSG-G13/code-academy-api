import dotenv from 'dotenv';

dotenv.config();

const {
  PORT,
  DEV_DB_URL,
  NODE_ENV,
  SECRET_KEY,
  EMAIL,
  EMAIL_PASSWORD,
  BUCKET_NAME,
  BUCKET_REGION,
  AWS_ACCESS_KEY_ID,
  AWS_SECRET_ACCESS_KEY,
  DATABASE_URL,
} = process.env;

export {
  PORT,
  DEV_DB_URL,
  NODE_ENV,
  SECRET_KEY,
  EMAIL,
  EMAIL_PASSWORD,
  BUCKET_NAME,
  BUCKET_REGION,
  AWS_ACCESS_KEY_ID,
  AWS_SECRET_ACCESS_KEY,
  DATABASE_URL,
};
